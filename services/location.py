import requests

# converts address to coordinates. returns a list of longitude and latitude
def convert_address(address):
    coordinates = []
    if not address:
        raise Exception("Invalid Address")
    
    # call api
    # coordinates = LOCATION API

    
